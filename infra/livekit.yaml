# Минимальный конфиг LiveKit для MVP.
# Для продакшена обязательно:
# - домен + TLS
# - корректный public IP / node_ip
# - STUN/TURN (ниже можно указать turn)
port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 61000
  port_range_end: 61999

# API keys для выдачи токенов (backend должен знать key/secret)
keys:
  devkey: "a3653aa6231e2e1a23bb684646b4c01708ea8fdd219d3b6e403f593cfe249782"

# TURN можно подключить так (после настройки coturn):
# turn:
#   enabled: true
#   domain: "your.domain"
#   tls_port: 5349
#   udp_port: 3478
#   external_tls: false
